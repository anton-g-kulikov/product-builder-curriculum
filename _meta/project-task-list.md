# Project Task List

This document owns active work, task status, and deferred repository follow-ups. Stable project guidance belongs in `README.md`; curriculum scope belongs in `CURRICULUM.md`; verification intent belongs in `test/test-documentation.md`.

## Active

### Bootstrap the metacoding repository workflow

- Status: in progress
- Goal: align the checked-in repository with the initialized metacoding workflow.
- Success criteria:
  - generated, vendor-specific metacoding copies remain local;
  - durable task and verification artifacts have distinct owners;
  - contributor guidance describes the workflow without duplicating its detailed rules;
  - repository checks pass;
  - granular commits are pushed to `origin/main`.
- In scope: Git ignore rules, workflow documentation, repository verification, commits, and push.
- Out of scope: curriculum lesson changes, application scaffolding, and a TypeScript test toolchain.
- Blocking subtasks: none.
- Deferred follow-ups: choose an automated Markdown checker when the repository gains package tooling or CI.

## Completed

- Initial curriculum repository created and published.
